一条毫无意义的使用说明
				by熊卓晨
=================================
src文件夹下有三个文件夹，其中cn为包含了四个类的包，rank为
存储了每一个模式的分数排行的文件，同时每场对局结束后的赢家分数存储路径也是rank文件夹内，resource文件夹为本次proj中的所有媒体资源，包括所有背景图片和BGM（及备用bgm），这些文件夹都应放在src文件夹下运行，所有资源的调用都是使用相对路径，只有cn文件夹一个包。
       其中应该运行的main文件是MainFrame.java，个人存档的存储路径是src文件夹。